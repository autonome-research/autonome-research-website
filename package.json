{
  "name": "autonome-research",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-content.mjs",
    "dev": "npm run generate && vite",
    "build": "npm run generate && vite build && node scripts/validate-site.mjs",
    "test": "node --test",
    "check": "npm test && npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^18.0.6",
    "parse5": "^8.0.1",
    "sanitize-html": "^2.17.6"
  }
}
